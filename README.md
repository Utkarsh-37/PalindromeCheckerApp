UC6 – Queue + Stack Hybrid

Stores the same characters in both Queue (FIFO) and Stack (LIFO).
Compares dequeue vs pop results to check for symmetric matching.
Great for understanding how different data structures behave with identical data.
Combines two contrasting structures to demonstrate deeper logic flow.
