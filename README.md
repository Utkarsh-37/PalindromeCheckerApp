UC1 – Application Entry

Initializes the application and prints a welcome message along with version info.
Serves as the main entry point from where the JVM begins execution.
Establishes a clean startup flow to prepare the system for later logic.
A simple foundational use case that sets the tone for the rest of the project.
