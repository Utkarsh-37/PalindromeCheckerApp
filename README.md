UC8 – LinkedList‑Based Approach

Adds characters into a LinkedList and removes from both ends to compare.
Leverages dynamic node-based structure for flexible operations.
Reinforces linked list strengths in symmetric access scenarios.
A practical use case that blends data structure behavior with algorithmic logic.
